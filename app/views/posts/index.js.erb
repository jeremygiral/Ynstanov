$('#post').append("<%= escape_javascript(render 'post')%>");
$('#paginator').html("<%= escape_javascript(link_to_next_page(@posts, 'Load More', id: 'load_more'))%>");
if (!$('#load_more').length) { $('#paginator').remove(); }
